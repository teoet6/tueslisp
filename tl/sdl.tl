(define open-window (extract-function '../libs/sdl.so 'open_window))
(define close-window (extract-function '../libs/sdl.so 'close_window))
(define fill-rect (extract-function '../libs/sdl.so 'fill_rect))
